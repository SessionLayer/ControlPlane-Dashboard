# F-s27-4: audit write-path caveat removed with an unverifiable in-repo claim

- Severity: medium
- Status: Verified-Fixed
- Area: api-truth

## Issue

Rebuilding the Audit log screen, a teammate correctly identified that the
5-dimension audit search's write-path caveat (dating from before the S20
backfill) was stale, and removed it along with its regression test — but the
replacement doc comment asserted the dimensions were "proven end-to-end at
S20's cross-repo E2E closure" with no citation any future maintainer (or this
repo's CI) could verify. The underlying claim is true (confirmed against
`Docs/sessions/twenty/RESULT.md` §3: S20 Part B backfilled all 5 dimensions,
closing S18's `F-audit-write-dimensions-1`, regression-tested by CP's
`AuthorizeIT`), but an unverifiable assertion in a security-adjacent (RBAC
label-scoping) code comment is exactly the kind of thing that silently rots.

Found by the T5 api-truth reviewer.

## Resolution — Verified-Fixed

`src/features/observability/AuditFilters.tsx`'s doc comment now cites the
actual evidence: the S18 finding id, the S20 Part B backfill, the regression
test name, and a pointer to `Docs/sessions/twenty/RESULT.md` §3 — traceable,
not just asserted.
